// src/utils/firebase.js
// This file would contain your Firebase client SDK initialization if you were
// using Firebase services like Firestore, Authentication, Cloud Storage, etc.,
// directly in your React app.

// For deployment with Firebase Hosting, this file is not directly involved.
// The configuration for hosting is primarily in firebase.json.

// Example of what it *could* look like if you used client-side Firebase:
/*
import { initializeApp } from "firebase/app";
// import { getAnalytics } from "firebase/analytics"; // Uncomment if using Analytics

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
  // measurementId: "G-YOUR_MEASUREMENT_ID" // Uncomment if using Analytics
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
// const analytics = getAnalytics(app); // Uncomment if using Analytics

// Export app and other services if needed in your components
// export { app, analytics };
*/

console.log("Firebase client-side utility file included. Configure here if using Firebase services beyond Hosting.");